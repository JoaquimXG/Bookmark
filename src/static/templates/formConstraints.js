const credential = {
    password: ".+",
    name: ".+",
};

const contact = {
    email: "(^[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+.[A-Za-z]{2,}$|^(?![\\s\\S]))", 
}

const location = {

}

const asset = {
}

const backup = {

}

export default {
    asset,
    credential,
    location,
    backup,
    contact
};
