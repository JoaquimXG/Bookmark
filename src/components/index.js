import React from "react";
import { NavBar } from "./NavBar";
import { PrimaryCard } from './PrimaryCard'

export const Home = () => {
    return (
        <div>
            <NavBar />
            <body>
                <PrimaryCard />
            </body>
        </div>
    );
};
